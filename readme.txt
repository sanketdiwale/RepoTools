RepoManagement should be added as a submodule to your repo as:


The scripts in this folder can then called to execute some of the standard workflow operations described in http://nvie.com/posts/a-successful-git-branching-model/ on that repo.